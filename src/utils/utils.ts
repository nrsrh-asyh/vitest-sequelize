// functions that use the models go here

// okay. how to write functions that use models?

const User = require("../../db/models/user");
const Admin = require("../../db/models/admin");

export const testFunc = () => {
  // how tf to use sequelize models
};
